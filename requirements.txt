# Core dependencies for persistent agent system
# All dependencies are optional based on deployment needs

# Core
pyyaml>=6.0                    # YAML policy input support

# Optional: LLM API integrations (uncomment as needed)
# openai>=1.0.0              # OpenAI GPT-4, GPT-3.5
# anthropic>=0.18.0          # Claude 3 (Opus, Sonnet, Haiku)
# cohere>=4.0.0              # Cohere models

# Optional: Semantic search with embeddings
# sentence-transformers>=2.3.0  # Local embeddings (all-MiniLM-L6-v2)
# faiss-cpu>=1.7.4              # Efficient vector search
# numpy>=1.24.0                  # Required for embeddings

# Optional: Production features
# pydantic>=2.0.0            # Enhanced data validation
# structlog>=23.0.0          # Structured logging
# prometheus-client>=0.19.0  # Metrics export

# Optional: Advanced persistence
# sqlalchemy>=2.0.0          # Advanced ORM if needed
# redis>=5.0.0               # Distributed caching
# asyncpg>=0.29.0            # PostgreSQL async driver

# Development dependencies
# pytest>=7.4.0              # Testing
# pytest-asyncio>=0.21.0     # Async test support
# black>=23.0.0              # Code formatting
# mypy>=1.7.0                # Type checking
# ruff>=0.1.0                # Fast linting
